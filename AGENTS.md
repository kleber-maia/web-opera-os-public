# Repository instructions

- When sharing a local development preview, never provide a `localhost` URL. Use the LAN `Network` URL printed by Vite instead.
- When asked to push changes, push the current branch to the existing `origin` remote. Do not create or deploy a separate hosted preview unless explicitly requested.
